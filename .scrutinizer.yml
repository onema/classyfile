build:
    tests:
        override:
            -
                command: 'phpunit --coverage-clover=code-coverage'
                coverage:
                    file: 'code-coverage'
                    format: 'php-clover'
